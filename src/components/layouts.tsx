import { type ParentProps } from 'solid-js'

export function SmallLayout(props: ParentProps<unknown>) {
  return <div>{props.children}</div>
}

export function MediumLayout(props: ParentProps<unknown>) {
  return <div>{props.children}</div>
}

export function LargeLayout(props: ParentProps<unknown>) {
  return <div>{props.children}</div>
}
